<%= form_with model: [grand_prize, praise], local: false do |f| %>
	<%= f.text_field :comment, id: "comment_textarea" %>
	<%= button_tag type: "submit", class: "btn btn-sm btn-info" do %>
		<i class="fa-regular fa-paper-plane"></i>
	<% end %>
<% end %>
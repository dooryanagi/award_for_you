ユーザ名や家族情報を編集できる

<%= form_with model: @user, url: update_information_path do |f| %>

  <%= f.label :name, "ユーザー名" %>
  <%= f.text_field :name %>

  <%= f.submit %>

<% end %>

<%= link_to "卒業する", users_unsubscribe_path %>